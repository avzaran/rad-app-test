# Backend

## Run locally

1. Install Go 1.23+
2. `go mod tidy`
3. `go run ./cmd/api`

Default port: `8080`.

Current implementation runs on in-memory repositories with SQL migrations prepared in ./migrations.

